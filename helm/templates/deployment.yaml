---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: headscale-operator
  labels:
    app.kubernetes.io/name: headscale-operator
    app.kubernetes.io/part-of: headscale
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: headscale-operator
  replicas: {{ .Values.deployment.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  revisionHistoryLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: headscale-operator
        app.kubernetes.io/part-of: headscale
    spec:
      serviceAccountName: headscale-operator
      terminationGracePeriodSeconds: {{ .Values.deployment.terminationGracePeriodSeconds }}
      containers:
        - name: headscale-operator
          image: {{ .Values.steiger.headscaleOperator.image }}
          args: ["/bin/headscale-operator"]
          env:
            - name: RUST_LOG
              value: "info,headscale_operator={{ .Values.deployment.logLevel }}"
            - name: TLS_CERT_PATH
              value: /run/secrets/tls
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8443
              name: webhook
              protocol: TCP
          volumeMounts:
            - name: webhook-tls-certs
              mountPath: /run/secrets/tls
              readOnly: true
          resources:
            requests:
              cpu: 10m
              memory: 20Mi
            limits:
              cpu: 40m
              memory: 80Mi
      volumes:
        - name: webhook-tls-certs
          secret:
            secretName: webhook-tls
