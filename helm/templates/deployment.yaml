---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: headscale-operator
  labels:
    app.kubernetes.io/name: headscale-operator
    app.kubernetes.io/part-of: headscale
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: headscale-operator
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  revisionHistoryLimit: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: headscale-operator
        app.kubernetes.io/part-of: headscale
    spec:
      serviceAccountName: headscale-operator
      terminationGracePeriodSeconds: 15
      imagePullSecrets:
      - name: github-registry
      containers:
        - name: headscale-operator
          image: {{ .Values.steiger.headscaleOperator.image }}
          args: ["/bin/headscale-operator"]
          env:
            - name: RUST_LOG
              value: "info,headscale_operator=debug"
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 10m
              memory: 20Mi
            limits:
              cpu: 40m
              memory: 80Mi
