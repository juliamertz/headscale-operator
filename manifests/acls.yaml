apiVersion: headscale.juliamertz.dev/v1
kind: Policy
metadata:
  name: example
spec:
  headscaleRef:
    name: example
  rules:
    - action: accept
      src: ['kubernetes@']
      dst: ['homelab@:80,433']
    - action: accept
      src: ['julia@']
      dst: ['*:*']
