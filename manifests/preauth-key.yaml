apiVersion: kubus.io/v1
kind: PreauthKey
metadata:
  name: example
spec:
  ephemeral: true
  reusable: true
  expiration: 99999h
  targetSecret: tailscale-auth
  userId: 3
  headscaleRef:
    name: headscale-example
    # namespace: ""

